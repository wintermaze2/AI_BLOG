<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Lightweight JSP Blog</display-name>

    <!-- 정적 리소스(/static/*)는 컨테이너 기본 서블릿이 서빙.
         DispatcherServlet 이 "/" 를 잡아도 정적 파일은 여기로 우회된다. -->
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/static/*</url-pattern>
    </servlet-mapping>
    <!-- robots.txt / favicon 등 루트 정적 파일도 기본 서블릿이 서빙 (프론트 컨트롤러 우회) -->
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/robots.txt</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/favicon.ico</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/favicon-192.png</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/apple-touch-icon.png</url-pattern>
    </servlet-mapping>

    <!-- DispatcherServlet, SitemapServlet, RssServlet, Listener 는 애노테이션으로 등록됨 -->

    <!-- ===================================================================
         관리자 필터 체인. 애노테이션(@WebFilter)으로 선언하면 필터 간 실행 순서가
         스펙상 보장되지 않으므로, 순서가 중요한 이 둘만 web.xml에 명시한다.
         web.xml의 filter-mapping은 선언 순서대로 적용된다.

           1) adminIpFilter  : 허용 IP가 아니면 여기서 404로 끊는다
           2) authFilter     : 통과한 요청에 대해 세션 로그인 여부 확인
         =================================================================== -->
    <filter>
        <filter-name>adminIpFilter</filter-name>
        <filter-class>com.example.blog.filter.AdminIpFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>adminIpFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>authFilter</filter-name>
        <filter-class>com.example.blog.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>authFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>

    <welcome-file-list>
        <welcome-file>index</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
